# flow:vibevm-github-smoke

Test-only flow. See `spec/flows/vibevm-github-smoke/PROTOCOL.md`.

This boot snippet exists purely so the publish path has a non-trivial
boot artefact to round-trip. It carries no real instructions for the
session.
