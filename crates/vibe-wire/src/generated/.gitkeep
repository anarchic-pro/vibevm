# Files in this directory are generated by `cargo xtask codegen` from
# JTD schemas at the repo root under `schemas/`. Do NOT edit by hand —
# any change is overwritten on the next regeneration. CI asserts via
# `cargo xtask check-codegen` that what's checked in here matches what
# `cargo xtask codegen` would produce from the current schemas.
