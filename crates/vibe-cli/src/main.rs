//! The `vibe` CLI — entry point. Thin dispatcher: parse args, route to
//! the appropriate crate, render the result. The implementation lands in
//! a follow-up commit.
//!
//! Spec: `VIBEVM-SPEC.md` §9.

#![forbid(unsafe_code)]

fn main() {}
